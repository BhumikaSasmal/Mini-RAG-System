# Task 6: Mode Comparison & Manual Test Log 

| Test Area | Scenario / Input Question | Expected Result | Actual Result | Status | Remarks |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **LLM mode - TXT** | Question based on `sample_policy.txt`: *"What is a sample policy?"* | Grounded answer with TXT source and page `N/A`. | **Answer:** *"A sample policy is a foundational document that outlines the rules, principles, and expectations..."* <br>Source: `sample_policy.txt` (Page: `N/A`, Chunk: `sample_policy.txt_c0`) | **Pass** | Gemini synthesized text context cleanly into a cohesive explanation. |
| **LLM mode - PDF** | Question based on `sample_report.pdf`: *"What is the document about?"* | Grounded answer with correct PDF source metadata. | **Answer:** *"The document provides an overview of a sample policy, explaining its foundational role in outlining rules..."* <br>Source: `sample_report.pdf` (Page: `1`, Chunk: `sample_report.pdf_p1_c0`) | **Pass** | Metadata extraction and page numbers accurate. |
| **Mock vs LLM** | Same question executed in both modes: *"What is the document about?"* | Differences documented without changing retrieval source set unnecessarily. | Both modes retrieved `sample_report.pdf_p1_c0` and `sample_policy.txt_c0`. Mock mode returned raw 2-sentence extractions; Gemini mode generated a structured overview of all key components. | **Pass** | Highlights clear quality gap between raw extractive sentences and LLM reasoning. |
| **Insufficient Context** | Question unrelated to indexed documents: *"Who wrote Harry Potter?"* | Clear insufficient-context response. | **Answer:** *"The available document context is insufficient to answer this question."* | **Pass** | Safely triggers strict grounding fallback string without hallucinating. |
| **Source Traceability** | Successful RAG query: *"What are roles and responsibilities?"* | Displayed source matches retrieved chunk used for context. | **Answer:** *"In a sample policy, roles and responsibilities clarify who is responsible for what..."* <br>Source: `sample_report.pdf` (Page: `2`, Chunk: `sample_report.pdf_p2_c3`) | **Pass** | Complete alignment between retrieved context chunk and UI metadata preview. |
| **Missing Credential** | LLM mode selected without valid credential (`GEMINI_API_KEY` unset). | Controlled message; application remains usable. | Sidebar displays ` GEMINI_API_KEY is not set in your environment!` error banner; application interface stays responsive. | **Pass** | Non-blocking warning banner prevents system crashes. |
| **Provider/API Failure** | Simulated or observed API failure (e.g., key error or quota issue). | Clear error status and no unhandled crash. | Output displays: `Error communicating with LLM Provider: ...` inside an error container. | **Pass** | Exceptions caught gracefully in `src/llm_service.py`. |
| **Multi-document Query** | Relevant content distributed across indexed TXT/PDF: *"What are the review and update requirements?"* | Appropriate source retrieved and answer remains grounded. | **Answer:** *"Based on the provided document, policies must be updated to reflect new requirements..."* <br>Sources: `sample_policy.txt` and `sample_report.pdf` | **Pass** | Multi-source context blended cleanly into a single answer. |
| **Repeat Query** | Same question submitted more than once after re-indexing. | Consistent behavior and no duplicate indexing side effect. | UI displays *"No new chunks were added. Document is already indexed."* and yields identical answer. | **Pass** | Deduplication logic prevents duplicate database entries. |
